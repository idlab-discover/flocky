package utils

/*func GetPort(node string) int {
	port := config.Cfg.DeploymentPort
	if config.Cfg.TestMode {
		nodenumber, _ := strconv.Atoi(node[1:])
		port += nodenumber
	}
	return port
}*/

/*func GetIP(ip net.IP) net.IP {
	if config.Cfg.TestMode {
		return net.ParseIP(config.Cfg.DeploymentIP)
	} else {
		return ip
	}
}*/
